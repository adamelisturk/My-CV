version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/srv/app
    env_file:
       - .env
    command: python manage.py runserver 0.0.0.0:8000
